<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="95dp">

    <TextView
        android:layout_width="100dp"
        android:id="@+id/courseId"
        android:textSize="20sp"
        android:layout_marginStart="20dp"
        android:text="@string/course_id"
        android:textColor="@color/black"
        android:layout_marginEnd="20dp"
        android:maxLines="1"
        android:layout_marginTop="15dp"
        android:layout_height="wrap_content"/>

    <ImageView
        android:id="@+id/courseIconFinder"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginStart="20dp"
        android:layout_below="@+id/courseId"
        android:layout_marginTop="7dp"
        android:contentDescription="@string/course_icon"
        app:srcCompat="@drawable/course_iconfinder"/>

    <TextView
        android:id="@+id/courseName"
        android:layout_toEndOf="@+id/courseIconFinder"
        android:layout_below="@+id/courseId"
        android:layout_width="330dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:layout_marginEnd="20dp"
        android:layout_marginStart="10dp"
        android:text="@string/course_name"
        android:textSize="16sp"/>

    <TextView
        android:id="@+id/cardBorder"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginStart="15dp"
        android:layout_marginEnd="15dp"
        android:layout_alignParentBottom="true"
        android:background="#808080"/>
</RelativeLayout>